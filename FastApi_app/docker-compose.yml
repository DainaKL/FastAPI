services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    command: sh -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000"
